# 开发文档

## 1. 项目概述

查寝小帮手是一个使用 Tkinter 构建的桌面程序，用于按寝室名单生成卫生检查模拟评分，并导出为管理学院院公检记录表样式的 Excel 工作簿。应用逻辑集中在 `查寝小帮手ultra.py` 一个 Python 脚本中。导出样式由脚本生成，运行时不需要携带参考工作簿。

程序的评分算法与原有实现保持一致。本次表格适配只增加年级字段，并把已有结果转换为“每间寝室一列”的工作表。

## 2. 环境与依赖

- Python 3.9 或更高版本
- Tkinter（通常随 Python 一起提供；Linux 可能需单独安装系统 Tk 包）
- `numpy`：正态分布随机数
- `pandas`：汇总评分和计算统计反馈
- `openpyxl`：创建并格式化 `.xlsx` 工作簿

安装 Python 包：

```bash
python -m pip install numpy pandas openpyxl
```

运行：

```bash
python "查寝小帮手ultra.py"
```

## 3. 单脚本结构

`查寝小帮手ultra.py` 包含以下部分：

- `SCORING_ITEMS`：评分项目名称与满分值。
- `REPORT_ROWS`：Excel 中评分项顺序、分组和评分结果键之间的映射。
- `export_inspection_workbook(...)`：创建工作簿、填写元信息和评分、设置合并单元格与格式并保存文件。
- `UltimateDormScoreApp`：构建 Tkinter 界面、读取输入、生成评分、汇总结果并向用户反馈。
- `if __name__ == "__main__"`：创建窗口并启动 Tkinter 事件循环。

界面输入和 Excel 输出由导出函数连接；评分结果以字典列表传递，`generate_scores` 中的随机评分和统计计算不依赖工作簿布局。

## 4. 界面输入

| 字段 | 默认值或格式 | 用途 |
| --- | --- | --- |
| 检查日期 | 当前日期，格式为 `YY.MM.DD` | 写入工作表检查日期信息 |
| 检查人员 | 空 | 写入工作表检查人员信息 |
| 所属年级 | 空 | 写入工作表年级信息 |
| 随机种子 | `666`；留空则不指定种子 | 控制 NumPy 和 Python `random` 的随机序列 |
| 最低分、最高分 | `84`、`96` | 计算总分目标的正态分布区间 |
| 寝室列表 | 每行一个寝室号 | 决定生成的寝室记录及 Excel 列数 |
| 保存路径 | 当前工作目录下的默认 `.xlsx` 文件名 | 工作簿输出位置 |

点击“恢复默认寝室”会将列表替换为脚本内的三个示例寝室号。检查日期、人员、年级和寝室号按文本写入 Excel；评分和总分按数值写入。

## 5. 原有评分算法

每次生成时，程序先把最低分和最高分转换为整数，并检查最低分小于最高分。指定随机种子时，程序分别初始化 NumPy 和 Python `random`。对每个寝室：

1. 以最低分和最高分的中点作为正态分布均值，以区间宽度的六分之一作为标准差，抽取一个目标总分，并裁剪到用户设置的区间。
2. “礼貌”固定为 5 分。满分为 5 分的其他项目从 3、5 中随机取值；满分为 10 分的项目从 6、8、10 中随机取值。
3. 最多随机调整其他项目 50 次，每次加减 2 分；10 分项目的下限为 6，5 分项目的下限为 3，调整不超过项目满分。当当前分数与目标分数相差不超过 1 时提前停止。
4. 当前各项得分之和作为输出总分。

生成完成后，程序继续使用 pandas 计算总分平均值、标准差和各项目平均值，并通过完成对话框显示。算法的随机过程、参数、迭代次数和统计口径保持原样。

### 评分项及表格说明

| Excel 行项目 | 结果字典键 | 满分 |
| --- | --- | ---: |
| 礼貌情况 | `礼貌` | 5 |
| 床上物品被子、被单、枕头摆放 | `床上` | 10 |
| 床下鞋子、行李及物品摆放 | `床下` | 10 |
| 个人衣物及洗漱用品摆放 | `个人衣服` | 10 |
| 地面干净，无脏水、纸屑等杂物 | `地面卫生` | 10 |
| 电网线不乱挂或纠缠 | `电线` | 10 |
| 窗台无垃圾杂物 | `窗台` | 5 |
| 不乱挂杂物、乱贴字画 | `乱挂杂物字画` | 10 |
| 桌椅摆放整齐 | `桌椅` | 5 |
| 桌面物品摆放整齐 | `桌面` | 10 |
| 卫生间干净无异味 | `厕所` | 10 |
| 及时清理垃圾 | `及时清理垃圾` | 5 |

各行的中文说明和先后顺序由 `REPORT_ROWS` 管理，分组满分由 `SCORING_ITEMS` 管理。参考工作簿中的历史分数只是样例；导出使用算法生成的分数，不复制参考文件中的单元格数据。

## 6. Excel 字段映射与版式

导出工作簿只有一个名为“院公检记录表”的工作表，标题为“管理学院寝室卫生检查”。A、B 列用于分组和项目说明，寝室从 C 列开始横向排列。每个寝室列包含寝室号、12 项分数、“无”备注和总分。分项与总分为整数单元格。

| 信息 | 工作表位置或规则 |
| --- | --- |
| 表格标题 | 第 2 行，从 A 列合并至最后一个寝室列 |
| 检查日期、所属年级、检查人员 | 7 间及以上时位于第 3 行；寝室少于 7 间时占第 3–5 行 |
| 寝室号 | 表头行，C 列起每间寝室一列 |
| 12 项评分 | 表头后连续 12 行，项目说明在 B 列，分数在对应寝室列 |
| 备注 | 评分项之后，A:B 合并显示“备注(有无拒检现象)”，各寝室列写入“无” |
| 总分 | 备注下一行，A:B 合并显示“总分(100分)”，各寝室列写入算法计算的总分 |

寝室少于 7 间时，日期、年级和检查人员分别占用完整的独立行，以避免三个字段在窄表头中重叠。7 间及以上时，表头采用参考表横向排列的元信息布局。寝室列按实际数量生成：1 间对应 C 列，超过 11 间时继续延伸至后续列。工作表使用 Calibri 字体、细边框、居中换行、分组纵向合并和打印区域；超过 11 间时改为横向页面方向。

导出列与结果字典的对应关系如下：

- 寝室号：`寝室`
- 分项：按 `REPORT_ROWS` 中的键读取评分
- 总分：`总分`
- 检查日期、所属年级和检查人员：分别由界面字段传入导出函数
- 备注：固定为 `无`

## 7. 校验、错误处理与验证

输入校验包括最低分必须小于最高分、随机种子必须可转换为整数（或留空）、寝室列表不能为空。生成和文件保存过程中的异常由界面捕获，并通过错误对话框展示。日期、检查人员、年级格式以及重复寝室号当前不做额外校验。

版本修改时应验证以下场景：

- 使用同一随机种子、参数和寝室名单，逐寝室各项分数及统计值应与原算法一致。
- 使用 1、3、7、11、12 间寝室分别导出，确认单工作表、表头布局、寝室列数量、12 项评分、备注和总分正确。
- 重新打开输出文件，确认寝室号仍为文本，评分和总分为数字，合并区域、打印区域及页面方向有效。
- 在没有参考 `.xlsx` 文件的目录中运行导出，确认应用不依赖模板文件。

## 8. 已知限制

- 每个寝室的备注统一为“无”，界面暂不支持逐寝室备注或拒检状态编辑。
- 寝室数量超过 11 间时工作表会继续向右扩展；单页打印时可能缩小内容，建议打印前在 Excel 中预览分页。
- 随机评分算法没有修改；最低分和最高分约束的是目标总分抽样范围，并不保证最终分数一定落在该区间。
- `pandas` 对单个寝室计算样本标准差时会得到 `NaN`，完成反馈沿用原有统计行为。
